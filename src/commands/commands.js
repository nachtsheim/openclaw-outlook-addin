// Commands file for add-in command surface (ribbon buttons).
// Currently unused — reserved for future command extensions.

Office.onReady(() => {
  // No-op: commands initialization placeholder
});
